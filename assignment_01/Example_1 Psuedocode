# Write an R function that reads two numbers, multiplies them together and prints out their
# product. You could start out with multiply two <- function(num 1, num 2) f ... g.

Create function that reads two numbers
  multiple two numbers 
  calculate product 
print 
